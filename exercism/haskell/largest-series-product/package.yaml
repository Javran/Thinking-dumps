name: largest-series-product
version: 1.2.0.6

dependencies:
  - base

library:
  exposed-modules: Series
  source-dirs: src
  ghc-options: -Wall
  dependencies:
    - mtl

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - largest-series-product
      - hspec
