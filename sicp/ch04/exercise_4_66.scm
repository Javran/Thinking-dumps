(load "../common/utils.scm")
(load "../common/test-utils.scm")

;; planning to have a new handler:
;; (lisp-eval function return-var arg1 arg2 ...)
;; where (function arg1 arg2 ...) is evaluated
;; as lisp expression, and the result is pattern matched
;; against return-var. and then we can write lisp
;; functions to do whatever we want.

(end-script)

;; Local variables:
;; proc-entry: ""
;; End:
