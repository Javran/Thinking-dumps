(load "../common/utils.scm")
(load "../common/test-utils.scm")

; I'm afraid there's no simple way of setting a random seed
;   using a specified number, let's try to do this in racket.

(end-script)
