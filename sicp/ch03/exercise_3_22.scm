(load "../common/utils.scm")
(load "../common/test-utils.scm")

(define (make-queue)
  (let ((front-ptr 'todo)
        (rear-ptr 'todo))
    'todo
    (define (dispatch m)
      'todo)
    dispatch))


(end-script)
