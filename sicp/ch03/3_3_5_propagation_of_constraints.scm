(load "../common/utils.scm")
(load "../common/test-utils.scm")

; construct a constraint network
;   that a single change in the network
;   will cause all related variables to be changed
;   accordingly

; primitives:
; * adder
; * multiplier
; * constant

(end-script)
