(load "../common/utils.scm")
(load "../common/test-utils.scm")

;; * does "spread-argument" only spread
;;   operands to "arg1" and "arg2"?
(define (spread-arguments operands)
  'todo)

(end-script)

;; Local variables:
;; proc-entry: ""
;; End:
