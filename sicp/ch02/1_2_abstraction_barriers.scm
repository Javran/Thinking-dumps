(load "../common/utils.scm")

; * isolate different "levels" of the system
; * procedures at each level are the interfaces
;     that define the abstraction barriers
;     and connect the different levels
