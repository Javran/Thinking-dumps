% init
:- initialization(['common.pl']).
:- initialization(run).

% facts

% Task #1: find predication that can read/write variables

% queries
query( format('thats it!~n',[]) ).

% dummy query
query( true ).

% dummy query
query_u( '_=_.' ).
